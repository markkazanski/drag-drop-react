import React, { Component } from 'react';

class DealButton extends Component {
    state = {

    };

  render() {
    return (
        <button>
            Deal Card
        </button>
    );
  }
}

export default DealButton;
